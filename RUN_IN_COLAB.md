# Run in Google Colab

1. Open `notebooks/01_climate_crop_yield_business_analysis.ipynb`.
2. Upload/clone the whole repository so the `src/` package is available.
3. Install dependencies:

```python
!pip install -q pandas numpy matplotlib seaborn scikit-learn
```

4. Run all cells from top to bottom.
5. Save the executed notebook as an additional validation copy if desired.

The notebook downloads source data at run time, so internet access is required.
